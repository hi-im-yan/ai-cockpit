import type { Project } from "./types";

/**
 * Seed projects for a brand-new install (no saved store yet). Empty now that the app is
 * real — you start from a clean slate and add your own projects via "+ Project".
 */
export const seedProjects: Project[] = [];
